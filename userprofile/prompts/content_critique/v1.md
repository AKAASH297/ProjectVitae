You will receive a pre-computed list of keyword-coverage issues (JD terms missing from draft content). Do NOT modify those entries.

Add only semantic-match issues: tone, alignment, exaggeration, missing context.

Output `ContentCritiqueOutput` with `issues` list. Each issue must have:
- `location`: section id or "global"
- `kind`: "content_keyword" or "formatting"
- `note`: description
- `keyword_match`: null for semantic issues
- `phase`: "content"
