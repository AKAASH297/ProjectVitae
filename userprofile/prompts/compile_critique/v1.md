You will receive the filled LaTeX source of the resume. Judge ATS-friendliness: section ordering, single-column readability, no exotic packages, consistent date formats, page-break risk.

Output `CritiqueResult` with:
- `issues`: list of issues, each with `phase="compile"`
- Each issue must have location, kind ("formatting" or "content_keyword"), note
